// This is empty and is just here to trigger a request from the client to the server
// So the hook can check if the user is authenticated
// Without this, a logged out user can navigate to /protected during CSR navigation
